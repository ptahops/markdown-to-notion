{
  "name": "markdown-to-notion",
  "version": "1.0.0",
  "description": "GitHub Action to sync Markdown docs to Notion pages",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --source-map --license licenses.txt",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@notionhq/client": "^2.2.15",
    "@tryfabric/martian": "^1.2.4"
  },
  "devDependencies": {
    "@types/node": "^22.13.1",
    "@vercel/ncc": "^0.38.3",
    "typescript": "^5.7.3"
  }
}
